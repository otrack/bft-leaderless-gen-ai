INVARIANT Validity Integrity Consistency

INIT Init

NEXT Next

CONSTANTS
    DEBUG = FALSE
    CmdSet = {"A", "B", "C", "D"}
    ValSet = {"OK"}
    InitState = 0    
    ProcSet = {p1, p2}
    ClientSet = {p1}
    f = 1

CHECK_DEADLOCK FALSE